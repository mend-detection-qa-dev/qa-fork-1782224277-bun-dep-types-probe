{
  "name": "bun-dep-types-probe",
  "version": "0.1.0",
  "description": "Mend SCA detection probe — covers dep-types-basic and trusted-dependencies patterns. One package per dependency group.",
  "private": true,
  "dependencies": {
    "hono": "^4.12.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  },
  "optionalDependencies": {
    "fsevents": "^2.3.0"
  },
  "trustedDependencies": [
    "fsevents",
    "esbuild"
  ]
}